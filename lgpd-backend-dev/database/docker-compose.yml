version: '3.8'

services:
  banco-lgpd:
    image: eriklisboa1/postgres-lgpd-populado:1.0
    container_name: banco-lgpd
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: LGPD
    restart: always
